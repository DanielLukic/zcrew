#!/usr/bin/env bash
# zcrew-managed
set -euo pipefail
pane_id="${1:-}"
shift || true
text="${*:-}"
[[ -n "$pane_id" && -n "$text" ]] || { echo "usage: tell <pane_id> <text>" >&2; exit 1; }
source "$(dirname "$(realpath "${BASH_SOURCE[0]}")")/multiplexer.sh"
mx_send_text "$pane_id" "$text"
